The repo contains a Signal Processing pipeline that takes in a corrupted image as input and transforms a few features to produce a clean blueprint

The matlab file, corrputed image, transformed images at each stage and final image are included
